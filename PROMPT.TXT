Prompt :  I need to build a simple transaction backend using Python and Flask. Can you help me write a single-file app with SQLAlchemy? I need two models: a Wallet which should store the balance in cents as an integer to avoid math errors and an Order For the logic, I need an admin route to credit or debit wallets, please make sure to use with for update to lock the row so I don't get race conditions. Then, I need a client route to place orders. It should first lock the wallet and deduct the money securely, commit that transaction, and only then call a mock external fulfillment function (just a dummy sleep timer  to get a tracking ID. Finally, update the order record with that ID.
